const myName = "hwanhee";

if (myName == "hwanhee") {
  console.log("Hello!");
} // parenthesis, curly braces

let isLoggedIn = true;

if (isLoggedIn == false) {
  alert("User is logged in!");
  console.log("User is logged in!");
} else {
  console.log("User is NOT logged in!");
}

const enteredUserName = "hh";

if (enteredUserName) {
  //empty string or 0(zero) is converted to false
  console.log("Input is valid!");
}
